{
    "name": "@securitize/sui-sdk",
    "version": "5.3.1",
    "type": "module",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "import": "./dist/index.js"
        }
    },
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsup",
        "dev": "tsup --watch",
        "prepare": "pnpm build",
        "prepublishOnly": "pnpm build",
        "changeset": "changeset",
        "version": "changeset version",
        "deploy_localnet": "rm -f ./Pub.localnet.toml && sui client switch --env localnet && cross-env NETWORK=localnet MVR_FALLBACK_NETWORK=testnet pnpm exec tsx ./src/scripts/deploy.ts",
        "deploy_devnet": "rm -f ./Pub.devnet.toml && sui client switch --env devnet && cross-env NETWORK=devnet MVR_FALLBACK_NETWORK=testnet pnpm exec tsx ./src/scripts/deploy.ts",
        "deploy_testnet": "sui client switch --env testnet && cross-env NETWORK=testnet pnpm exec tsx ./src/scripts/deploy.ts --",
        "deploy_mainnet": "sui client switch --env mainnet && cross-env NETWORK=mainnet pnpm exec tsx ./src/scripts/deploy.ts",
        "deploy_devnet_test_token": "sui client switch --env devnet && cross-env NETWORK=devnet pnpm exec tsx ./src/scripts/deploy_test_token.ts",
        "deploy_testnet_test_token": "sui client switch --env testnet && cross-env NETWORK=testnet pnpm exec tsx ./src/scripts/deploy_test_token.ts",
        "exp": "sui client switch --env devnet && cross-env NETWORK=devnet pnpm exec tsx ./src/scripts/test_registerInvestor.ts",
        "test": "NODE_OPTIONS='--experimental-vm-modules' jest --runInBand",
        "lint": "npx prettier -c **/*.ts",
        "fix": "npx prettier -c **/*.ts --write",
        "clean": "rm -rf ./node_modules"
    },
    "devDependencies": {
        "@changesets/cli": "^2.31.0",
        "@jest/types": "^30.4.1",
        "@types/jest": "^30.0.0",
        "jest": "^30.4.2",
        "prettier": "^3.8.3",
        "ts-jest": "^29.4.10",
        "tsup": "^8.5.1"
    },
    "dependencies": {
        "@iarna/toml": "^2.2.5",
        "@mysten/bcs": "^2.0.5",
        "@mysten/codegen": "^0.8.5",
        "@mysten/move-bytecode-template": "^0.3.2",
        "@mysten/sui": "^2.17.0",
        "@types/node": "^24.12.4",
        "cross-env": "^10.1.0",
        "dotenv": "^17.4.2",
        "tsx": "^4.22.3",
        "typescript": "^5.9.3"
    }
}
