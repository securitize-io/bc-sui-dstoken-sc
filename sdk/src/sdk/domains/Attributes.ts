export enum AttributeType {
    NONE = 0,
    KYC_APPROVED = 1,
    ACCREDITED = 2,
    QUALIFIED = 4,
    PROFESSIONAL = 8,
}

export enum AttributeStatus {
    PENDING = 0,
    APPROVED = 1,
    REJECTED = 2,
}